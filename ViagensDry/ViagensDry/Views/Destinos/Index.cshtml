@model IEnumerable<ViagensDry.Models.Destino>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LugarDestino)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DiaPartida)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LugarDestino)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DiaPartida)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDestino">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDestino">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDestino">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
